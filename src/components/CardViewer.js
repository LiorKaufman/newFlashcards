import React, { Component } from 'react';

export class CardViewer extends Component {
  constructor(props) {
    super(props);

    this.state = {};
  }

  render() {
    return (
      <div>
        <h2>We are in the card viewer</h2>
      </div>
    );
  }
}

export default CardViewer;
